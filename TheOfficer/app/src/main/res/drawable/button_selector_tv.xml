<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Focused state for TV navigation -->
    <item android:state_focused="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/button_focused" />
            <stroke android:width="4dp" android:color="@color/primary_light" />
            <corners android:radius="8dp" />
        </shape>
    </item>
    
    <!-- Pressed state -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/button_pressed" />
            <corners android:radius="8dp" />
        </shape>
    </item>
    
    <!-- Selected state (for programmatic highlighting) -->
    <item android:state_selected="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/button_focused" />
            <stroke android:width="3dp" android:color="@color/primary_light" />
            <corners android:radius="8dp" />
        </shape>
    </item>
    
    <!-- Default state -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/button_default" />
            <corners android:radius="8dp" />
        </shape>
    </item>
</selector>
